<aside class="fixed left-0 top-0 h-full w-64 bg-surface-container-low dark:bg-surface-container-lowest shadow-sm flex flex-col py-unit gap-4 z-50">
    <div class="px-6 py-8">
        <h1 class="font-headline-md text-headline-md font-bold text-primary">RecipeHub Chef</h1>
        <p class="text-on-surface-variant font-label-md text-label-md opacity-70">Chef Dashboard</p>
    </div>

    <nav class="flex-1 flex flex-col gap-1 px-4">
        <a class="{{ active == 'dashboard' ? 'bg-secondary-container text-on-secondary-container rounded-xl font-bold flex items-center px-4 py-3 shadow-sm' : 'text-on-surface-variant flex items-center px-4 py-3 hover:bg-surface-container-high transition-all rounded-xl group' }}" href="{{ path('app_chef_dashboard') }}">
            <span class="material-symbols-outlined mr-3 {{ active == 'dashboard' ? '' : 'group-hover:scale-110 transition-transform' }}" data-icon="dashboard">dashboard</span>
            <span class="font-label-md">Dashboard</span>
        </a>

        <a class="{{ active == 'profile' ? 'bg-secondary-container text-on-secondary-container rounded-xl font-bold flex items-center px-4 py-3 shadow-sm' : 'text-on-surface-variant flex items-center px-4 py-3 hover:bg-surface-container-high transition-all rounded-xl group' }}" href="{{ path('app_chef_profile') }}">
            <span class="material-symbols-outlined mr-3 {{ active == 'profile' ? '' : 'group-hover:scale-110 transition-transform' }}" data-icon="person">person</span>
            <span class="font-label-md">Profil</span>
        </a>

        <a class="{{ active == 'tags' ? 'bg-secondary-container text-on-secondary-container rounded-xl font-bold flex items-center px-4 py-3 shadow-sm' : 'text-on-surface-variant flex items-center px-4 py-3 hover:bg-surface-container-high transition-all rounded-xl group' }}" href="{{ path('app_tags_index') }}">
            <span class="material-symbols-outlined mr-3 {{ active == 'tags' ? '' : 'group-hover:scale-110 transition-transform' }}" data-icon="label">label</span>
            <span class="font-label-md">Tags</span>
        </a>

        <a class="text-on-surface-variant flex items-center px-4 py-3 hover:bg-surface-container-high transition-all rounded-xl group" href="{{ path('app_home') }}">
            <span class="material-symbols-outlined mr-3 group-hover:scale-110 transition-transform" data-icon="home">home</span>
            <span class="font-label-md">Accueil</span>
        </a>
    </nav>

    <div class="px-4 py-6 mt-auto flex flex-col gap-1 border-t border-outline-variant/20">
        <a class="text-on-surface-variant flex items-center px-4 py-3 hover:bg-error/10 hover:text-error transition-all rounded-xl" href="{{ path('app_logout') }}">
            <span class="material-symbols-outlined mr-3" data-icon="logout">logout</span>
            <span class="font-label-md">Déconnexion</span>
        </a>
    </div>
</aside>